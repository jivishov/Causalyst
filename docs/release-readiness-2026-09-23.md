# Assessment release readiness — 23 September 2026

This is a read-only preflight of the deployed installation plus preparation for its next release. It is **not** a deployment record or a passing teacher evaluation. Public deployment: `https://jivishov.github.io/Causalyst/`; Worker variable: `https://alt-assessment-student-api.emil-jivishov.workers.dev`; hosted Supabase project ref: `bpqckntsnattswwzmroq` (project display name `AlterMent`). The deployed student Google button handed off to Google with this project as the OAuth target. Existing Worker Actions deployment predates the release branch.

| Gate | Verified evidence | Outstanding |
| --- | --- | --- |
| Database migrations | **Production** migration history records the two `20260921` hardening files; `assessment_versions`, `attempts.assessment_version_id`, and `attempt_artifacts.frozen_at` are absent there. An empty, isolated **Causalyst Staging** Supabase project (`nscgqhsfudbnzfayvrnl`) was created and all 28 committed SQL files applied in order using hosted migrations. Its history has 28 records, with 19 public tables, private retention holds and grade history present. Four expected Storage buckets are private. All six transactional SQL suites passed on **hosted staging PostgreSQL**, including a synthetic service-role hold/cleanup claim, then rolled back; profiles, attempts, artifacts and holds remained at zero. The old-schema replay with 2,300 synthetic draft uploads also passed locally. | The staging database has zero application records; it cannot prove the historical production backfill or full Auth/Storage/Worker journey. Six `20260922` release migrations and the final retention-hold migration remain unapplied in **production**. Earlier numbered files are reflected in production schema without history; never replay blindly. Hosted connector assigned application timestamps as history **versions** while preserving source migration **names/order**. Reconcile this staging history against source filenames before a future CLI `db push`; do not assume it is interchangeable with file versions. |
| Data safety | The production project is on Supabase Free with **no managed backups**. There are 2,300 uploaded legacy draft artifacts older than 30 days and one pending draft artifact; the uploaded objects exist in private Storage. Zero unexpired nonterminal voice sessions/jobs were observed at preflight time. | Obtain and verify a **private database plus Storage backup and a restore test** before production DDL. Include Auth identities, `public`, Storage metadata and objects, and new `app_private` retention holds after cutover. Use an assessment maintenance window; old Worker submission is incompatible with the new SQL. |
| Staging and OAuth | Public app loads. Production Supabase site URL is `https://jivishov.github.io/Causalyst/`; configured allowed redirects include deployed login, teacher and password-reset paths plus localhost development paths. Google navigation from the app-owned button worked. A separate hosted staging **database** now exists with the schema and private bucket checks above. | No staging frontend/Cloudflare Worker or staging Google OAuth configuration has been deployed. Google sign-in was stopped before credentials; callback, teacher/student access, enrollment and deployed provider sessions remain **unverified**. A pre-auth Google handoff is not OAuth acceptance. |
| Provider | Existing GitHub repository has a secret named `OPENAI_API_KEY`. [The first probe run](https://github.com/jivishov/Causalyst/actions/runs/35802021604) stopped at module resolution before any API request; it gave **no provider entitlement result**. [PR #8](https://github.com/jivishov/Causalyst/pull/8) moved the probe into the Worker workspace; [its validation passed](https://github.com/jivishov/Causalyst/actions/runs/35802311438). The corrected [main-only probe succeeded](https://github.com/jivishov/Causalyst/actions/runs/35802572739) at commit [`74c9f4e`](https://github.com/jivishov/Causalyst/commit/74c9f4e7af703e044a841a1a993e07f92443b85b): its [job](https://github.com/jivishov/Causalyst/actions/runs/35802572739/job/106996010806) reported catalog visibility for `gpt-5.5`, `gpt-4o-transcribe`, and `gpt-realtime`, then completed one bounded synthetic Responses request with returned model `gpt-5.5-2026-04-23`. | Catalog visibility does **not** establish transcription or Realtime execution. The Responses probe does not certify the deployed Worker, Cloudflare sideband, deadline finalization, hangup, or grading quality. Run those journeys in an isolated deployed environment with synthetic authenticated accounts. |
| Teacher grading | Live gradebook has 8 entries with zero AI-approved scores, zero teacher overrides and zero published grades; 1 of 15 attempts has a provisional AI score. A synthetic chemistry review packet and fail-closed evaluator are prepared. | No independent teacher labels or agreed modality-level thresholds exist. A teacher must score actual handwriting/audio/live captures before model results are revealed; evaluate matching output and safety cases, then document supervised classroom acceptance. |

The hold migration preserves every previously unhashed pending/uploaded/processed draft artifact ID in `app_private.legacy_artifact_retention_holds`, clears its Storage deletion deadline, and makes SQL cleanup claims refuse held IDs. It does not hold new uploads. The existing six migration bytes remain unchanged. Apply the final hold migration in the same maintenance window as the six pending SQL files, before enabling the new Worker cron. Never mark GitHub's `migrations_applied` input true until the hosted migration, staging and backup steps actually pass; that input cannot inspect the database itself.

Hosted staging permissions were checked: RLS is enabled on `app_private.legacy_artifact_retention_holds`; `anon` and `authenticated` lack schema usage, `authenticated` cannot read assessment snapshots or execute artifact cleanup, and `service_role` can read but not insert retention holds. The staging hold table is empty because the project has no historical artifacts; the local upgrade fixture verifies the populated backfill/claim path. Staging security advisor reported only informational RLS-without-policy notices on intentionally private tables. Performance advisor reported informational unindexed foreign keys and unused indexes on the fresh empty schema; review these with workload evidence rather than treating unused-index notices as findings from production.

[PR #7](https://github.com/jivishov/Causalyst/pull/7) merged the retention and evaluation preparation after its [validation passed](https://github.com/jivishov/Causalyst/actions/runs/35801762899); [PR #8 validation](https://github.com/jivishov/Causalyst/actions/runs/35802311438) passed for the provider-probe packaging correction. Both validation jobs also [passed on merged main at `74c9f4e`](https://github.com/jivishov/Causalyst/actions/runs/35802502683), covering 367 unit tests, native PostgreSQL migrations and concurrency, real disposable Supabase integration, generated database types, browser tests, build, and security checks. Hosted staging SQL suites exercise PostgreSQL and Supabase-owned schemas, while application/provider integration beyond the bounded Responses probe still requires a deployed staging Worker/frontend. The production project has real records; do not export them into public GitHub Actions artifacts or source control.
