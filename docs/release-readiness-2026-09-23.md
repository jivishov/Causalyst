# Assessment release readiness — 23 September 2026

**Current status:** all seven pending production SQL migrations are applied, and [Deploy Worker run 35814334827](https://github.com/jivishov/Causalyst/actions/runs/35814334827) **succeeded** at `cbd4933c78bab99d120bcde84817870c600bd580`. The matching Worker and frontend are deployed, the deployment runner's Worker health check passed, and the live student sign-in page loads. The user stated **"Actually I don't need any previous data in the db"**, waiving the historical-data backup/restore prerequisite for this cutover; no backup was completed. The existing records were not cleared. The workflow's migration acknowledgement covers completed hosted schema and predeployment staging checks under that waiver; it does not certify signed-in classroom or real voice acceptance.

The table below retains historical preflight findings alongside the updated production result. Public app URL: `https://jivishov.github.io/Causalyst/`; Worker variable: `https://alt-assessment-student-api.emil-jivishov.workers.dev`; production Supabase project: `bpqckntsnattswwzmroq` (AlterMent). A previous deployed student Google button handed off to Google with this project as the OAuth target, but that did not complete sign-in.

| Gate | Verified evidence | Outstanding |
| --- | --- | --- |
| Database migrations | **Production:** all seven pending SQL files were applied in order through hosted migrations from exact committed bytes. Post-migration checks found **2,301 legacy holds**, zero held artifacts with cleanup deadlines, four assessment versions, and zero attempts without snapshots. `authenticated` cannot SELECT assessments/versions or EXECUTE cleanup; `service_role` can SELECT versions. The matching Worker/frontend release succeeded. **Earlier staging evidence:** isolated `nscgqhsfudbnzfayvrnl` received all 28 files; six transactional suites and a 2,300-artifact local upgrade fixture passed. | The hosted staging database had zero application records and no deployed authenticated Worker/frontend journey. Older numbered files were reflected in production schema without history; never replay them blindly. The hosted connector uses application timestamps as history **versions** while retaining source migration names/order; reconcile before a future CLI `db push`. |
| Data safety | No data-clearing operation was planned or performed. The Supabase Free project reports **no managed backups**. The user waived historical-data backup/restore for this cutover: **"Actually I don't need any previous data in the db"**. The final hold migration protects all 2,301 current draft/unhashed pending/uploaded/processed artifacts; zero held rows have cleanup deadlines. Before migration, a quiet-window read found no active unexpired voice/generation work or attempt writes in the prior 30 minutes. | No backup/restore is certified. There was **no enforced maintenance mode**; the user was told to keep the app closed, which is distinct from a technical write lock. The old Worker/schema mismatch interval ended when the matching Worker was deployed. |
| Staging and OAuth | Public app loads. Production Supabase site URL is `https://jivishov.github.io/Causalyst/`; configured allowed redirects include deployed login, teacher and password-reset paths plus localhost development paths. Google navigation from the app-owned button worked. A separate hosted staging **database** now exists with the schema and private bucket checks above. | No staging frontend/Cloudflare Worker or staging Google OAuth configuration has been deployed. Google sign-in was stopped before credentials; callback, teacher/student access, enrollment and deployed provider sessions remain **unverified**. A pre-auth Google handoff is not OAuth acceptance. |
| Provider | Existing GitHub repository has a secret named `OPENAI_API_KEY`. [The first probe run](https://github.com/jivishov/Causalyst/actions/runs/35802021604) stopped at module resolution before any API request; it gave **no provider entitlement result**. [PR #8](https://github.com/jivishov/Causalyst/pull/8) moved the probe into the Worker workspace; [its validation passed](https://github.com/jivishov/Causalyst/actions/runs/35802311438). The corrected [main-only probe succeeded](https://github.com/jivishov/Causalyst/actions/runs/35802572739) at commit [`74c9f4e`](https://github.com/jivishov/Causalyst/commit/74c9f4e7af703e044a841a1a993e07f92443b85b): its [job](https://github.com/jivishov/Causalyst/actions/runs/35802572739/job/106996010806) reported catalog visibility for `gpt-5.5`, `gpt-4o-transcribe`, and `gpt-realtime`, then completed one bounded synthetic Responses request with returned model `gpt-5.5-2026-04-23`. | Catalog visibility does **not** establish transcription or Realtime execution. The Responses probe does not certify the deployed Worker, Cloudflare sideband, deadline finalization, hangup, or grading quality. Run those journeys in an isolated deployed environment with synthetic authenticated accounts. |
| Teacher grading | Live gradebook has 8 entries with zero AI-approved scores, zero teacher overrides and zero published grades; 1 of 15 attempts has a provisional AI score. A synthetic chemistry review packet and fail-closed evaluator are prepared. | No independent teacher labels or agreed modality-level thresholds exist. A teacher must score actual handwriting/audio/live captures before model results are revealed; evaluate matching output and safety cases, then document supervised classroom acceptance. |

The final hold migration ran after the preceding six migrations and before the new Worker cron. It keeps historic unhashed draft objects out of scheduled cleanup; new uploads retain ordinary deadlines. GitHub's `migrations_applied` input is an operator declaration, not a database inspection. In this run it means hosted schema checks and relevant staging predeployment gates passed under the explicit backup waiver; it does **not** mean classroom/OAuth/Realtime acceptance passed.

Hosted staging permissions were checked: RLS is enabled on `app_private.legacy_artifact_retention_holds`; `anon` and `authenticated` lack schema usage, `authenticated` cannot read assessment snapshots or execute artifact cleanup, and `service_role` can read but not insert retention holds. Staging security advisor's RLS-without-policy notices are informational for intentionally private tables; the production advisor similarly reports those notices. Production also reports an existing **Leaked Password Protection Disabled** Auth warning. Enable that protection when the Auth configuration is reviewed; see [Supabase password security guidance](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection). Staging performance advisor reported informational unindexed foreign keys and unused indexes on the fresh empty schema; review with workload evidence.

[PR #7](https://github.com/jivishov/Causalyst/pull/7) merged the retention and evaluation preparation after its [validation passed](https://github.com/jivishov/Causalyst/actions/runs/35801762899); [PR #8 validation](https://github.com/jivishov/Causalyst/actions/runs/35802311438) passed for the provider-probe packaging correction. Both validation jobs also [passed on merged main at `74c9f4e`](https://github.com/jivishov/Causalyst/actions/runs/35802502683), covering 367 unit tests, native PostgreSQL migrations and concurrency, disposable Supabase integration, generated types, browser tests, build, and security checks. Full signed-in OAuth, enrollment, submission, and real voice journeys remain unverified. Teacher grading acceptance is missing. A supervised **ungraded writing pilot** requires a completed flow with one teacher and two test students first. The production project has real records; do not export them into public Actions artifacts or source control.
